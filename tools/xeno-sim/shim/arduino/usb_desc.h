// Teensyduino's USB descriptor configuration. The simulator has no USB at all;
// the firmware only reads these to decide whether a Serial console exists.
#ifndef XENOSIM_USB_DESC_H_
#define XENOSIM_USB_DESC_H_
#endif
