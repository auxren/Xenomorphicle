// AVR's PROGMEM helpers. Everything they name is already defined by the
// Teensyduino stand-in (shim/arduino/Arduino.h), where a PROGMEM read is just
// a read: the host has one address space.
#include <Arduino.h>
